# Node
node_modules/
dist/
*.log
.DS_Store

# Nx
.nx/cache
.nx/workspace-data

# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*
wrangler.local.jsonc

# Vitest
coverage/

# Drizzle
# (migrations are committed; local sqlite state is not)
*.sqlite
*.sqlite-*

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Flutter/Dart cruft is handled by apps/mobile/.gitignore (+ generated
# apps/mobile/ios/.gitignore, web/.gitignore) from `flutter create`.

# Codegen output (regenerated from OpenAPI)
apps/mobile/lib/api/generated/

# Env / secrets
.env
.env.*

# FVM Version Cache
.fvm/
# Worker static assets (Flutter web build, assembled in CI)
apps/api/public/
