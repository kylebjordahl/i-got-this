# Flatten node_modules so shared tooling (vitest, wrangler, vitest-pool-workers)
# resolves cleanly across workspace packages.
shamefully-hoist=true
# Don't fail install on engine ranges; runtime is workerd, host is Node 22.
engine-strict=false
